#pragma once
#include <Windows.h>
class Misc
{
public:
	Misc();
	void radar(bool radarHax);
	void bhop(bool bhop);
	int playercheck(int playercheck);
	float velocity();
	void noflash(float flash);
	void Glow(bool glow);
};

